Event Delegate
: Event처리를 부모에게 위임하다
=> 이벤트 처리할 엘리먼트가 아직 존재하지 않을 때
     처음에 로딩시에는 존재하지 않다가,
     동적으로 생성된 엘리먼트에 이벤트를 걸 때 사용

===================================

Ajax(Asynchronous Javascript and XML)
=> 클라이언트 화면이 바뀌지 않은 상태에서, 서버쪽에 요청이 이루어지고
     그 결과가 클라이언트에게 반영됨.

=> 언어가 아닌 개발방식을 의미하는 용어.

=> google의 suggestion 기능이 대표적